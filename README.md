# idio-may-dev

Static landing page for EMESTUDIOS / idio.

## Deploy

This repo includes a GitHub Pages workflow. Push to `main`, then set the repository Pages source to **GitHub Actions** if it is not already enabled.
